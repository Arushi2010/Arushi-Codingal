// genarate a random number between 0 and 1 

const randomNum=Math.random();
console.log(`Random number between 0 and 1: ${randomNum}`);

// genarate a random number between 1 and 10

const randomInt=Math.floor(Math.random()*10)+1;
console.log(`Random number between 1 and 10: ${randomInt}`);